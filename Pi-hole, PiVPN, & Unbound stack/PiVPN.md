# PiVPN
1. Download & run the one-step automated installer.
    ```
    curl -L https://install.pivpn.io | bash
    ```
2. Choose Wireguard when prompted.
3. Select a port for Wireguard to listen on (default is 51820)